import React from 'react';
import { Meta } from '@storybook/react';
import {ProjectForm} from './ProjectForm';

export default {
  title: 'PropertyInvest/ProjectForm',
  component: ProjectForm
} as Meta;

export const Standard = () => <ProjectForm />
